#version 420
#extension GL_ARB_separate_shader_objects : enable

// shader c92c1c4c0a2fb839
// Used for: Camera Depth of Field Blur
// Neutralised by discarding. The original already discards every pixel it considers in focus
// and only writes the out-of-focus ones, which are then blended over the sharp image, so
// discarding unconditionally leaves that sharp image everywhere.

layout(location = 0) in vec4 passParameterSem2;
layout(location = 0) out vec4 passPixelColor0;

void main()
{
discard;
}
