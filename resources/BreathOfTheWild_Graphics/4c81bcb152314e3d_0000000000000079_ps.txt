#version 430
#extension GL_ARB_separate_shader_objects : enable

// shader 4c81bcb152314e3d
// Draws the last flare sprite per eye, the one whose colour is lifted by (1 - fade).
// One of the three shaders behind the sun's lens flare; see 31ad25f9c41f4552 for why they are
// stubbed. All three blend additively, so a zero export removes the sprite and nothing else.

layout(location = 0) noperspective in vec4 passParameterSem0;
layout(location = 1) noperspective in vec4 passParameterSem1;
layout(location = 0) out vec4 passPixelColor0;

void main()
{
passPixelColor0 = vec4(0.0);
}
